#include "pch.h"
#include "CppUnitTest.h"
//#include "../TaskManagerVSC++/Task.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace Test
{
	/*TEST_CLASS(Test)
	{
	public:
		
		TEST_METHOD(TestCopyTask)
		{*/
			//TODO: make Copy in Task public and start this test
			/*Task tasks;
			stack<string>* result, *copy;

			copy = new stack<string>;
			copy->push("1");
			copy->push("2");
			copy->push("3");

			result = tasks.Copy(copy);

			Assert::IsTrue(result->top() == "3", L"Assert 1");
			result->pop();

			Assert::IsTrue(result->top() == "2", L"Assert 2");
			result->pop();

			Assert::IsTrue(result->top() == "1", L"Assert 3");
			result->pop();

			Assert::IsTrue(result->size() == 0, L"Assert 4");*/

	/*	}
	};*/
}
